package com.foreshock.negten.repository;

import com.foreshock.negten.model.NegotiationTender;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NegotiationTenderRepository extends JpaRepository<NegotiationTender, Long> {
}
